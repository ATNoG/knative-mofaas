---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: mofaasfunctions.mofaas.atnog
spec:
  group: mofaas.atnog
  names:
    kind: MoFaaSFunction
    listKind: MoFaaSFunctionList
    plural: mofaasfunctions
    singular: mofaasfunction
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: MoFaaSFunction is the Schema for the mofaasfunctions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MoFaaSFunctionSpec defines the desired state of MoFaaSFunction
            properties:
              concurrency:
                description: Concurrenty will default to 1 if not defined
                type: integer
              variants:
                items:
                  properties:
                    apiVersion:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    revisionName:
                      type: string
                  required:
                  - kind
                  - name
                  - revisionName
                  type: object
                type: array
            type: object
          status:
            description: MoFaaSFunctionStatus defines the observed state of MoFaaSFunction
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
